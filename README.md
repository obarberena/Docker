# Docker
En este repositorio, se realiza la instalación de Docker en Centos 7 (VM).
Dentro del mismo se realiza las siguientes tareas:

Realizar un pull de imagen Docker con apache.
Correr el contenedor y hacer forward del puerto 80 para que el index.html se vea desde tu maquina.
